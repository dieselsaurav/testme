<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:textColor">@color/text</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:actionBarStyle">@style/AppActionBar</item>
    </style>


    <!-- Action Bar -->
    <style name="AppActionBar" parent="android:Widget.Holo.ActionBar">
        <item name="android:titleTextStyle">@style/AppActionBarTitleText</item>
        <item name="android:background">@color/action_bar_background</item>
        <item name="android:displayOptions">showTitle</item>
    </style>

    <style name="AppActionBarTitleText" parent="android:TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/action_bar_title_text</item>
    </style>

</resources>
